# Database (obligatoire pour Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/course_platform"

# API Nest (optionnel, défaut: 3001)
# Frontend Next tourne sur 3000 → utiliser 3001 pour l'API afin d'éviter les conflits.
PORT=3001
